import React from "react";
import { BrowserRouter } from "react-router-dom";
import Menu from "./Menu";

const Root = () => (
  <BrowserRouter>
    <Menu />
  </BrowserRouter>
);

export default Root;
